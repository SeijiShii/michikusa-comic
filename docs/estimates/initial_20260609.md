---
generated_at: 2026-06-09
generator: /flow:estimate
context: whole
source_input: ./docs/concept.md
interpreted_as:
  feature: null
  phase: rough
  source: autodetect
phase: rough
confidence_band:
  ai_impl: "±300%"
  human_bottleneck: "±50%"
  ai_tokens: "±50%"
nfr_profile:
  scale: low
  throughput: low
  latency: standard
  availability: none
  source: explicit  # concept §3 NFR（個人サービス、SLA なし、同時数十〜数百）
calibration:
  global_metrics: empty (0 sessions) → デフォルト係数
  pj_internal: none (STATS.md 不在) → グローバル 100%
  nfr_multiplier: 0.70  # scale low 0.7 × throughput low 0.8 × latency standard 1.0 × availability none 0.9 ≈ 0.50 を、機能ロジックは規模非依存のため 0.70 に緩和適用
summary:
  min:
    total_files: 48
    total_lines: 2700
    human_hours: 10
  standard:
    total_files: 105
    total_lines: 6500
    human_hours: 22
  full:
    total_files: 205
    total_lines: 13500
    human_hours: 46
---

# 見積もり: 道草コミック（全体・初回フェルミ推定）

> **SCENARIO Phase 1 estimate 1回目**。concept §1.3（機能 9 + 横断 9）+ §3 NFR からの初回推定。
> phase=**rough**（SPEC 未生成、概念設計のみ）。実績メトリクス未蓄積のため confidence band は広い（AI-impl **±300%**）。
> Phase 2 で最初の 1 feature 完了後に `/flow:estimate`（2回目）で実績取り込み再キャリブレ（`refined_<date>.md`）。

## サマリ表

| スコープ | ファイル数 | コード行数 (logic+test) | 人間時間 | AI 推論トークン (設計+実装) |
|---|---|---|---|---|
| Minimum | 48 | 2,700 | 10h | 設計 130K / 実装 ~700K = 合計 ~830K (±50%) |
| Standard | 105 | 6,500 | 22h | 設計 260K / 実装 ~1.8M = 合計 ~2.06M (±50%) |
| Full | 205 | 13,500 | 46h | 設計 420K / 実装 ~3.6M = 合計 ~4.0M (±50%) |

> **confidence band**: AI-impl ±300%（rough）/ Human-bottleneck ±50% / AI 推論トークン ±50%（実装込み）
> **NFR プロファイル**: scale=low, throughput=low, latency=standard, availability=none（concept §3 由来、明示）
> **金額換算**: コード行数 × AI コーディングレート、AI 推論トークン × モデル単価（その時期で要確認）で利用側が算出
> **参考単価（2026-06 時点想定、要確認）**: Haiku 4.5 input $0.80 / output $4、Sonnet 4.6 input $3 / output $15、Opus 4.7 input $15 / output $75（per 1M tokens）

## 1. 機能フォルダ別ブレークダウン（Standard 基準、logic+test LOC）

| フォルダ | 主タスク | classification | Std files | Std LOC | 根拠 |
|---|---|---|---|---|---|
| capture | ui_code/api_code/unit/e2e | ai_impl | 8 | 500 | アップロード + EXIF/エリア/日付メタ + ひとこと + 入力検証（SEC-005） |
| compose | ui_code/api_code/unit/e2e | ai_impl | 16 | 1,150 | **最重量**: AI 4 コマ生成 + パネル SVG/Canvas 合成 + セリフ編集 + 保存 + 生成ステータスポーリング（[論点-001/002]） |
| gallery | ui_code/api_code/unit/e2e | ai_impl | 8 | 500 | 時系列/エリア別一覧 + 詳細（開示の履行も兼ねる） |
| collection | api_code/ui/unit | ai_impl | 5 | 320 | 月次自動コレクション + 集計バッチ |
| share | ui_code/helper/unit/e2e | ai_impl | 7 | 450 | 合成画像レンダリング + OS 標準シェア + OG カード |
| export | ui/api/unit/e2e | ai_impl | 8 | 550 | 高画質再生成 + Stripe 単発 + 書き出し |
| feedback | ui/api/unit | ai_impl | 6 | 350 | 好き嫌い + バグ報告 + PII scrub（O40/O28） |
| legal | doc/ui | ai_impl | 5 | 220 | プラポリ/利用規約/特商法 静的ページ（O55 導線含む） |
| account | ui/api/cron/unit/e2e | ai_impl | 8 | 560 | **SEC-001/O54**: セルフ全データ削除（DB + R2 purge）+ 段階認証 UI + 開示閲覧 + AI 同意 + 自動 purge cron |
| **機能小計** | | | **71** | **4,600** | |

## 2. 横断フォルダ別ブレークダウン（Standard 基準、すべて is_new=true）

| フォルダ | classification | Std files | Std LOC | 根拠 |
|---|---|---|---|---|
| _shared/types | ai_impl | 3 | 150 | 作品/コマ/写真/ユーザー/課金の型 |
| _shared/helpers | ai_impl | 6 | 400 | 日付/エリア解決/画像リサイズ/EXIF/Zod 検証 |
| _shared/db | ai_impl | 7 | 500 | Neon + Drizzle スキーマ（8 エンティティ）+ マイグレーション |
| _shared/auth | ai_impl | 6 | 450 | Clerk ゲスト→段階認証 + owner resolver（SEC-004） |
| _shared/storage | ai_impl | 5 | 350 | R2 アップロード/署名 URL/キー設計/purge |
| _shared/ai | ai_impl | 8 | 600 | OpenAI Vision + Gemini ラッパ + プロンプト + フォールバック + レート制限（SEC-003） |
| _shared/cost-tracking | ai_impl | 5 | 350 | 呼び出しログ積算 + .env 単価 + 概算 + 無料枠アラート（§4.6.2） |
| _shared/payments | ai_impl | 5 | 400 | Stripe 単発 + Webhook 署名検証 |
| _shared/app-shell | ai_impl | 9 | 700 | 合成ルート + ルーティング + providers + API ハンドラ層 + Clerk セッション + PWA/deploy scaffold（O57） |
| **横断小計** | | **54** | **3,900** | |

> **二重計上回避**: 横断は全て新規（is_new=true）。機能側で参照する横断ロジック（auth/db/storage/ai）は横断側で 1 回のみ計上、機能側は配線分のみ。

## 3. 基本部分（12 項目、Standard）

| 項目 | Std files | Std LOC | 備考 |
|---|---|---|---|
| プロジェクト初期化 / Vite+PWA 設定 | 4 | 200 | manifest / SW / vite config |
| CI/CD（GitHub Actions: lint/typecheck/unit + Vercel） | 2 | 150 | §10.5 / §12.9 |
| インフラ・デプロイ（Vercel 設定 + env） | 2 | 120 | PaaS 完結 |
| 監視（Sentry + beforeSend PII マスク SEC-002） | 2 | 100 | §3.X SEC-002 |
| その他（i18n なし/a11y A/エラー/ログ/テスト基盤） | 6 | 280 | Standard 水準 |
| **基本小計** | **16** | **850** | |

> NFR multiplier 0.70 適用後、上記 3 セクション合計 ≈ (4,600 + 3,900 + 850) × 0.70 ≈ **6,500 LOC / ~105 files**（Standard）。Min = ×0.42 ≈ 2,700 / 48、Full = ×2.08 ≈ 13,500 / 205。

## 4. NFR 倍率の効き

| NFR 軸 | 値 | 倍率 | 影響 |
|---|---|---|---|
| scale | low | 0.7x | 同時数十〜数百、シャーディング/負荷分散不要 |
| throughput | low | 0.8x | バッチ/非同期生成、高スループット設計不要 |
| latency | standard | 1.0x | 生成は数十秒許容（非同期 UX） |
| availability | none | 0.9x | 個人サービス、SLA なし |
| **合計** | | **≈0.50 → 0.70 適用** | 機能ロジックは規模非依存のため過度な縮小を避け 0.70 で適用 |

## 5. 根拠サマリ

1. **compose が最重量**（Std ~1,150 LOC）: AI 生成 + パネル合成（アプリ側 SVG/Canvas、[論点-001]）+ 2 段階解像度（[論点-002]）+ ポーリング UX で他機能の 2 倍規模
2. **_shared/ai + cost-tracking が要**（Std 合計 ~950 LOC）: Vision + Gemini 二系統 + フォールバック + コスト積算（§4.6.2）+ 生成レート制限（SEC-003）が密結合
3. **account は SEC-001 由来の非交渉必須**（Std ~560 LOC）: セルフ削除 + R2 purge + 自動 purge cron。法令履行のため Min スコープでも削減不可
4. **app-shell（O57）を最後の合成 target として計上**（Std ~700 LOC）: 全部品の配線 + API ハンドラ層 + Clerk セッション確立がないと「動くアプリ」にならない
5. **NFR が低スケール**で全体を 0.70x 縮小: 個人サービス・無料枠前提で robustness コードを抑制

## 6. AI 推論トークン量（フェルミ推定）

**設計フェーズ（Standard, 完了分 + 残り）**:
- concept NEW（実績 ~13K）+ secure design（~11K）+ estimate（本書 ~8K）= 完了 ~32K
- 残り: feature/横断 設計 18 × ~10K = 180K + spec-review 18 × ~8K = 144K... を含むと過大。実運用では feature 設計は連続バッチで効率化 → 設計総計 **~260K（Std）**（±50%）
- Min（簡略設計）~130K / Full（spec-review + audit 厚め）~420K

**実装フェーズ（Standard）**:
- セッション norm ベース: tdd/e2e ~18 target × ~100K tokens/target ≈ **~1.8M tokens**（±50%）。skill 公式（LOC×1.5×loop）はコンテキスト再読を過小評価するため、セッション実績 norm を主とする
- Min ~700K / Full ~3.6M

> **金額換算は利用側に委ねる**（参考単価は冒頭参照）。トークン数が客観指標、単価変動から独立。

## 7. 注意点
- **rough フェーズ・実績未蓄積のため band は ±300%（AI-impl）**。あくまで桁感。SPEC 生成後の refined で精度向上
- NFR は concept §3 から**明示取得**（default ではない）
- Phase 2 で最初の feature（推奨: `_shared/db` or `capture`）完了後に `/flow:estimate`（2回目）で実績取り込み → `refined_20260609.md`
